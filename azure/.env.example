# Copy to azure/.env (gitignored). Loaded after root .env — same key here wins.
# Azure auth: az login or AZURE_CLIENT_ID / AZURE_CLIENT_SECRET / AZURE_TENANT_ID.

# Required for AKS discovery (omit if you only use --images-file)
AZURE_SUBSCRIPTION_ID=

# Optional
# AZURE_RESOURCE_GROUP=
